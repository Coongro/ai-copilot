---
'@coongro/ai-copilot': patch
---

La referencia de cada registro viaja también en el texto visible, como handle nominal `recurso:id`, y un `record` se proyecta aunque el repositorio devuelva un array de una fila (COONG-293).
