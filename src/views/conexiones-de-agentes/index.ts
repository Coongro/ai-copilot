/**
 * Conexiones de agentes — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `conexiones-de-agentes.view.ts`,
 * datos en `use-conexiones-de-agentes.ts`, lógica custom en `handlers.ts`.
 */
export { ConexionesDeAgentesView } from './conexiones-de-agentes.view.js';
