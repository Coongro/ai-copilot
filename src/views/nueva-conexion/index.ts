/**
 * Nueva conexión — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `nueva-conexion.view.ts`,
 * datos en `use-nueva-conexion.ts`, lógica custom en `handlers.ts`.
 */
export { NuevaConexionView } from './nueva-conexion.view.js';
